const URL = "http://localhost:3003";

const URL_API = `${URL}/api`;
const URL_PUBLIC_IMAGES_PRODUCTS = `${URL}/img/products`;
const URL_PUBLIC_IMAGES_USERS = `${URL}/img/users`;

module.exports = {
    URL,
    URL_API,
    URL_PUBLIC_IMAGES_PRODUCTS,
    URL_PUBLIC_IMAGES_USERS,
};
